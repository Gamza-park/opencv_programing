#include "opencv2/opencv.hpp"
#include "opencv2/core/types_c.h"
#include "opencv2/core/core_c.h"
//#include "opencv2/imgcodecs/imgcodecs_c.h"

using namespace cv;
using namespace std;

int main()
{
    /*Ptr<IplImage> oldImage(cvLoadImage("/home/park/opencv_programing/chapter3/lena.jpg",IMREAD_GRAYSCALE));
    if(oldImage.empty()){
        return -1;
    }

    Mat newImage = cvarrToMat(oldImage);

    imshow ("img",newImage);
    waitKey();*/

    return 0;
}